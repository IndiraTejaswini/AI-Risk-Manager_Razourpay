# Join-cardinality audit

Generated by `scripts/06_join_audit.py`. Deterministic.

- Generated (UTC): `2026-09-05 21:40:38`
- Matured population: 99,433 orders, `label_a` rate 2.972%, `label_b` rate 1.189%
- Risk set: 97,658 orders, `label_b` rate 1.210%
- Flag threshold: positive rate ≥ **3×** the panel rate

## Why this audit exists

`order_items` turned out to be a leak: 767 matured orders have no item rows and 100% of them are `label_a` positives, because an order that resolves to `unavailable` has no items in the export. Correcting for it dropped the secondary target's average precision from 0.2938 to 0.0190 (`eval/model_report.md` §5).

**Neither guardrail in this repo could see it.** The column whitelist checks names, and every column involved is admissible and genuinely knowable at checkout. The perturbation test blanks `order_delivered_carrier_date` and checks the matrix is unchanged — it inspects one column's *values*, not a join's *cardinality*. So this is a class of defect the existing checks are structurally incapable of catching, and it needs its own audit.

## 1. Every join, audited

"Zero joined rows" means the order reaches no row of that table by the stated path.

| Table | Join path | Orders with zero rows | `label_a` rate | vs panel | `label_b` rate | vs panel | Flag |
|---|---|---:|---:|---:|---:|---:|:--:|
| `geolocation (seller side)` | orders → items.seller_id → sellers.zip → geolocation.zip | 983 | 78.332% | 26.36× | 0.102% | 0.09× | 🚩 |
| `order_reviews` | orders.order_id → reviews.order_id | 768 | 15.885% | 5.35× | 11.068% | 9.31× | 🚩 |
| `order_items` | orders.order_id → items.order_id | 767 | 100.000% | 33.65× | 0.130% | 0.11× | 🚩 |
| `products` | orders → items.product_id → products.product_id | 767 | 100.000% | 33.65× | 0.130% | 0.11× | 🚩 |
| `sellers` | orders → items.seller_id → sellers.seller_id | 767 | 100.000% | 33.65× | 0.130% | 0.11× | 🚩 |
| `geolocation (customer side)` | orders.customer_id → customers.zip → geolocation.zip | 278 | 5.036% | 1.69× | 2.518% | 2.12× |  |
| `order_payments` | orders.order_id → payments.order_id | 1 | 0.000% | 0.00× | 0.000% | 0.00× |  |
| `customers` | orders.customer_id → customers.customer_id | 0 | — | — | — | — |  |

Same joins against the **risk set**, the population the primary target is defined on:

| Table | Orders with zero rows | `label_b` rate | vs risk-set panel |
|---|---:|---:|---:|
| `order_reviews` | 731 | 11.628% | 9.61× |
| `geolocation (customer side)` | 271 | 2.583% | 2.13× |
| `geolocation (seller side)` | 214 | 0.467% | 0.39× |
| `order_items` | 1 | 100.000% | 82.62× |
| `order_payments` | 1 | 0.000% | 0.00× |
| `products` | 1 | 100.000% | 82.62× |
| `sellers` | 1 | 100.000% | 82.62× |
| `customers` | 0 | — | — |

## 2. Findings

**5 of 8 joins flagged**, but they are not 5 separate defects and they do not carry equal weight. One is new, independent, and reaches the primary target; three are the `order_items` artifact seen through three joins.

### 🚩 `order_reviews` — a second, independent leak, and this one reaches the primary target

768 matured orders have no review row. Their `label_a` rate is 15.885% (5.35× panel) and their `label_b` rate is 11.068% (9.31× panel).

**On the risk set — the population the primary target is defined on — 731 orders are affected, with a `label_b` rate of 11.628% against a panel rate of 1.210%. That is 9.61×.**

Only 19 of the 768 are explained by the missing-items artifact, so this is an independent finding, not a restatement of it.

The mechanism is obvious once stated: **a customer who never receives their order does not review it.** Review presence is downstream of delivery, so its absence is a direct read on the outcome — and unlike the items artifact, it bites hardest exactly on the population we care about.

**No current feature is affected.** The feature builder never joins `order_reviews`; every review column sits in `REVIEW_COLUMNS` and is rejected by `assert_no_leakage`. Nothing in `eval/model_report.md` or `eval/significance.md` is contaminated by this.

**It is a landmine for the next step, and the whitelist as written does not stop it.** ARCHITECTURE.md §3.7 admits `review_score` *"as a customer-history feature if computed point-in-time from strictly prior orders"*. That allowance is correct and this audit does not overturn it — but it is about **prior orders' reviews**, which have resolved and are genuinely known at checkout. The distinction the whitelist currently leaves implicit:

> A **prior** order's review — its score, its existence, its absence — is admissible: that order resolved before the one being scored.
>
> **This** order's review is inadmissible in every form, and that includes its *absence*. A `has_review` flag, a review-row count, or any outer join that leaves nulls when no review exists, is the outcome.

A point-in-time customer-history feature built from reviews must therefore count only strictly-prior orders — which the existing PIT primitives already enforce — and must never fall back to a current-order review presence flag.

### 🚩 `order_items`, `products`, `sellers` — one defect, three joins

| Table | Orders with zero rows | Of which: no items at all | `label_a` rate |
|---|---:|---:|---:|
| `order_items` | 767 | 767 | 100.000% |
| `products` | 767 | 767 | 100.000% |
| `sellers` | 767 | 767 | 100.000% |

`products` and `sellers` are reached *through* `order_items`, so an order with no item rows reaches neither. All 767 in each case are exactly the orders that have no items — the same rows, counted three times. Fixing the item join fixes all three; there is no separate product-side or seller-side defect.

**The primary target is not exposed.** 1 order in the risk set of 97,658 is affected, because an order that reached a carrier had items. Already corrected for the secondary target in `eval/model_report.md` §5.

### `geolocation` — now load-bearing, and the seller side needs unpacking

**Customer side: 278 matured orders unreached, 1.69× on `label_a` and 2.13× on `label_b` within the risk set (271 orders). Below the 3× flag.**

**Seller side: 983 matured orders unreached at 78.332% `label_a` — 26.36×, which trips the flag. It is the items artifact wearing a third hat.** The join runs through `order_items`, so 767 of those 983 orders — 78% — have no item rows at all and never reached a seller to geocode. Removing them leaves 216 orders whose seller's zip prefix is genuinely absent from the reference table.

**On the risk set the seller side is inert**: 214 orders at 0.467% `label_b`, 0.39× the panel rate — *below* it, not above. That is the population the primary model trains and scores on, and it is where `route_distance_km` is used.

This entry stopped being hypothetical: `route_distance_km` (`eval/feature_expansion.md`) joins **both** endpoints to this table, which is why the seller side is audited separately — the customer-side entry alone would not have discharged the rule for a feature that needs both.

The argument for admissibility, made explicitly rather than assumed: `geolocation` is a **static reference table**. A zip prefix's coordinates do not depend on any order, and a prefix missing from it is a property of Brazilian postal coverage, fixed long before the order was placed. Its absence cannot be caused by an outcome that has not happened yet — which is the exact property the `order_items` absence lacked.

What that argument does **not** license: §4.5 lists geocoding confidence itself as a feature ("did the pincode resolve at all"). That is a row-presence feature by construction, and it is deliberately not built. `route_distance_km` is NaN where either endpoint fails to resolve and no flag is derived from the failure. The distinction is the whole point of this audit: reading a joined *value* is safe here, manufacturing a feature out of the *join's failure* is a separate claim that would need its own evidence.

- **`order_payments`** — 1 order with zero rows, positive rate 0.000% (0.00× panel). Below the 3× threshold.
- **`customers`** — every matured order reaches at least one row. No cardinality signal exists, so nothing can leak through it.

## 3. Are the availability features proxies for the same artifact?

The availability group exists to capture "this order came through a degraded path" (§4.1). On this panel a degraded path is *caused by* the outcome, so these are the features most likely to encode it. Measured on the matured population, where the artifact is live.

| Feature | Set when | n set | `label_a` rate when set | when clear | corr with `label_a` | corr with `label_b` |
|---|---|---:|---:|---:|---:|---:|
| `has_item_rows` | = 1 | 98,666 | 2.218% | 100.000% | -0.5038 | +0.0086 |
| `has_payment_row` | = 1 | 99,432 | 2.972% | 0.000% | +0.0006 | +0.0003 |
| `has_product_metadata` | = 1 | 98,650 | 2.218% | 97.957% | -0.4983 | +0.0087 |
| `has_zip_prefix` | = 1 | 99,433 | 2.972% | — | constant | constant |
| `n_missing_features` | > 0 | 96,470 | 3.016% | 1.519% | +0.4467 | -0.0036 |

**`has_item_rows` is the artifact, restated as a feature.** It is 0 for exactly the orders the `order_items` join misses, and every one of them is a positive. Its correlation with `label_a` is the whole finding in one number.

**`n_missing_features` inherits it.** The null pattern it counts is dominated by the columns that go null when the item join misses — `order_value`, `n_items`, `n_sellers`, `n_products`, `avg_item_price`, `freight_ratio` — so it is a continuous restatement of the same flag rather than an independent signal.

**`has_zip_prefix` is constant** and therefore carries nothing, which is the finding already recorded in `eval/feature_report.md`.

## 4. Consequences

1. **The `order_items` artifact does not reach the primary target.** The risk set requires a carrier date, and an order that reached a carrier had items — one order in 97,658 is affected. Every headline number in `eval/model_report.md` and `eval/significance.md` stands.
2. **The `order_reviews` artifact would reach it, and is not currently exploited.** 731 risk-set orders at 9.61× the panel rate. No feature joins the table today, and the whitelist rejects every review column, so nothing is contaminated — but §3.7's allowance for `review_score` as point-in-time history needs the prior-order / current-order distinction spelled out before that feature is built.
3. **The secondary target is exposed and is reported corrected.** Its usable average precision is 0.0190, not 0.2938.
4. **`has_item_rows` and `n_missing_features` must not enter a shipped model trained on the matured population.** They are admissible on the risk set, where the artifact does not exist, and inadmissible off it. That conditionality is the point: these are not universally-bad features, they are features whose validity depends on the population.
5. **A standing rule is added to `data/COLUMN_WHITELIST.md`**: any feature derived from join cardinality or row presence requires an audit entry here before use.

Findings are pinned in `tests/test_join_audit.py`, so a change in the data or the join paths surfaces as a test failure rather than as a quietly different report.

